# Vercel / local — client-safe vars only (NEXT_PUBLIC_* / VITE_*).
# Copy to .env.local for Vite dev; never commit real secrets.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_URL=

# Optional dev aliases (see vite.config.ts envPrefix)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
# VITE_API_URL=
